/**
 * Defines system render props
 *
 */

const RENDER_PROPS = {
  POLYGON_OUTLINE: { border: { color: "#aaa", width: 1 }, close: true },
  POLYGON_SELECTED_OUTLINE: { border: { color: "#14a", width: 1 }, close: true }
};

export default RENDER_PROPS;
